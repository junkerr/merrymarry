<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.mapper.MemberMapper">
	<select id="getMember3" parameterType="map" resultType="Member3">
		select * from member3
		<where>
			<if test="userid != null">login_userid=#{login_userid}</if>
			<if test="password != null">and login_password=#{login_password}</if>
		</where>
	</select>
</mapper>